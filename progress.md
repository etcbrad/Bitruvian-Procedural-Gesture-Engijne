Original prompt: The knee bending needs to be refined to fit the average walking cycle. Using Lutz and Richard Williams helps us refine this so every walk cycle matches the key poses and better fills in the blanks

- Current task: reduce excessive swing-leg knee lift on slow walks and make elbow motion scale with the same motion energy.
- Key constraint: keep one foot planted in walk cycles; the trailing foot should not rise too high on low-speed motions.
- TODO: verify the updated swing/stance timing visually on the 5057 build.
- TODO: check whether any other limb arcs still over-drive at low frequency.
- Verified a slow walk capture with the frequency slider genuinely reduced via the native input setter. Knee bend stayed compact and the trailing foot remained much lower than before.
- Current follow-up: start the app in WALK mode with a tighter stride band and only allow long strides when the character’s reach, frequency, and agility justify it.
- Tightened the walk stride envelope further so the startup pose should read as a compact walk rather than a long step.
- Verified startup after the synthesis change: default state opens in WALK mode with frequency near 1.0 and a visibly shorter stride.
- Follow-up: make the walk/jog/run buttons themselves describe the stride constraint they apply, and keep the synthesis envelope aligned with those visible ranges.
- Verified the mode toggles now display their constraint bands and switching to RUN updates the rendered gait state correctly.
- Updated ground awareness so bounce is derived from the support chain instead of a free body bob: foot contact, ankle drive, shin drive, thigh drive, landing, and push-off now feed the body Y correction.
- Verified the latest preview run after a clean rebuild: no page errors, fresh grounding state includes `supportLoad`, `groundBounce`, `leftChain`, `rightChain`, `leftCompression`, and `rightCompression`.
- Next tuning pass, if needed: adjust `GROUND_BOUNCE_FACTOR` and the landing/push-off phase windows to make the spring feel stronger or softer without reintroducing float.
- Removed the free-text prompt field and renamed the action to `Procedural Walk Cycle`.
- Fixed the procedural action to use the current gait mode ref, then verified the default click now logs `Procedural Cycle: WALK` instead of the stale `JOG`.
- Current follow-up: if we want stricter mode-specific demos, add a visible mode badge or preset text so walk/jog/run synthesis is obvious at a glance.
- Refined the lower-limb chain so the knee, shin, ankle, foot, and ground read as one connected action: foot-tip contact now uses the actual foot segment, stance loading/toe-off got smoother, and swing clearance is less abrupt.
- Verified the latest preview build in the browser harness with no page errors; the current screenshot shows a more grounded step with the lower-leg and foot alignment reading more naturally.
- Follow-up, if needed: tune stance toe-off vs swing clearance separately for `WALK`, `JOG`, and `RUN` so each mode reads more distinctly at a glance.
- Current pass: `toe_bend` now feeds the leg solver so the foot can articulate through stance, swing clearance, and release instead of remaining a dead channel.
- Current pass: shoulder swings now use a rounded pendulum curve with a stronger mode-aware envelope, and the text-state hook now reports both shoulder angles for verification.
- Next check: visually confirm the new foot roll and shoulder arc feel smoother rather than merely larger, then trim the coefficients if the motion reads over-driven.
- Verified in `output/web-game/foot-shoulder-pass-3`: foot articulation is more phase-aware, and shoulder swing now reaches a visibly larger arc in the state output (`armSwing` roughly `0.45`, shoulders around `±6.3` degrees).
- Remaining tuning note: if the arms still read too tucked on some characters, bump `calculateArmSwing` a bit more or add a small elbow counter-swing; the current pass is balanced but still conservative on the screen.
- Ground-awareness disabled for now: `applyFootGrounding` is temporarily a pass-through because the current grounding path is causing a visible mid-loop artifact and/or performance regression in the browser. Verified symptom is `grounding:null` while the mannequin still renders, so this is intentionally parked until contact detection, support locking, and bounce correction can be validated without frame drops.
- TODO: re-enable grounding once browser verification confirms stable contact detection and bounce correction, no frame drops on the current harness, and unit/integration tests pass; owner @bradleygeiser; target milestone `v0.3-grounding`.
- Foot fix: reduced the ankle overdrive in the locomotion solver, reintroduced per-foot smoothing, and clamped the foot roll range so the trailing foot keeps animating while the lead foot stops snapping into an upward pose.
- Verified with a browser time series after the rebuild: foot angles now stay in a narrower range, both feet continue to animate across the cycle, and the preview run stayed free of console/page errors.
- Current pass: lowered the foot drag and toe-roll influence again so the front foot sits closer to the ground and the push-off reads as a low rotational roll rather than a friction-like lift.
- Verified in the latest browser sweep at `/tmp/bitruvian-foot-audit-2`: foot angles landed around `-92` to `-99` instead of `-103` to `-108`, and the screenshots show the lead foot visibly lower through the pass window.
- Follow-up, if needed: soften the walk envelope defaults for `toe_bend` and `footDrag` now that the solver curve is in the right range.
- Added a key-pose authoring mode for walk cycles: the app now keeps a session-local `WalkKeyPoseSet`, supports capture/reset/mirror controls for contact/down/passing/up anchors, and overlays authored poses on top of the procedural gait only when the mode is enabled.
- Verified the new mode with the browser harness: overlay toggle, anchor selection, phase scrubbing, and capture/reset all updated the live `render_game_to_text` state, while exports now include the same key-pose payload as the preview.
- Reworked Key Pose Mode into an activation-based editor: activating snapshots the current live walk phase, freezes the cycle by default, and exposes a separate Play/Pause toggle plus a cycle scrubber for the live pose.
- Verified in-browser that activation selected the nearest anchor, frozen scrub moved the displayed pose while `keyPoseEditorPlaying=false`, and Play returned the editor to live-follow mode with `keyPoseEditorPlaying=true`.
- Cycle-derived update: the four visible anchors now seed from the current generated cycle, each anchor stores a `cyclePose` baseline, and the set also tracks a `cycleSeed` with hidden helper beats for Williams-style spacing.
- Added a `Resample from Cycle` action and wired anchor phase edits to re-sample the live generated pose, so authored changes stay additive while the cycle baseline follows the current gait.
- Verified activation again after the update: `render_game_to_text` now reports `cycleSeed`, `selectedAnchor.cycleData`, and the current anchor baseline, and the screenshot shows the editor frozen on the live cycle instead of a default fixed pose.
- TODO: if the harness needs deeper interaction coverage, add a test-friendly start-in-editor route or a dedicated automation hook for the key-pose panel before the next audit pass.
- Cleanup pass: removed the pose library and key-pose walk authoring modules, trimmed the gait menu back to the core walk/jog/run modes, and returned the app surface to the procedural gait plus advanced controls baseline.
- Verified the reset in-browser: the pose library and key-pose panel are gone, the Motion and Advanced gait controls remain, and `render_game_to_text` reports the core runtime pose/state without key-pose fields.
- Idle-mode pass: merged Idle into the Motion panel as a gait mode button beside Walk, nested the idle sliders under the gait controls, and removed the old top-bar locomotion/idle toggle split.
- Added a deterministic `window.advanceTime(ms)` hook so the Playwright web-game client can step the simulation in controlled increments for browser audits.
- Verified the idle capture at `output/web-game/idle-audit`: `motionMode` now reports `idle`, `lFoot`/`rFoot` are `-90`, `grounding` remains `null`, and the screenshot shows the feet planted and level in Idle mode with no console errors.
- Verified the mode switch back to Walk at `output/web-game/walk-audit`: `motionMode` returns to `locomotion`, the gait state renders normally, and the walk screenshot still shows a healthy cycle after the idle merge.
- UI reflow pass: merged the advanced gait sliders into the main Motion block, removed the nonfunctional pin toggles from Balance, and kept Idle as its own section beneath the motion stack.
- Verified with a scrolled sidebar capture at `output/web-game/idle-reorg-scroll`: the advanced sliders stay in Motion, Idle appears as a separate section below it, and the idle feet still render planted/horizontal.
- Final UI reflow: split the sidebar into `Shared` controls for mode + gravity center, `Running` controls for the locomotion sliders, and a dedicated `Idle` section beneath them. The dead pinning control was removed.
- Verified with `output/web-game/ui-reflow-audit`: the idle state still reports `lFoot`/`rFoot` as `-90`, `motionMode` is `idle`, and the scrolled sidebar shows Shared above Running above Idle with no console errors.
- TODO: decide whether to keep the hidden `idlePinnedFeet` state or remove it entirely now that the pinning UI is gone.
- Poser workspace pass: restored the live joint authoring surface with a `POSER` mode button left of the gait modes, brought back the body rotation control plus per-joint bend/stretch/FK sliders, and wired bone-body / pivot-dot dragging to the same pivot-offset state.
- Verified the new poser interaction with a direct browser drag on the right upper arm: `pivotOffsets.r_shoulder` changed from `0` to `18`, `workspaceMode` stayed `poser`, and the mannequin kept animating while the overlay updated live.
- TODO: if the poser needs to match the archived station more closely, pull in any remaining niche controls only after we confirm they can coexist with live procedural motion.
- Branding rename pass: changed the app title and visible header from Bitruvian to Bitruvius, updated the subtitle to "The Bitruvian Posyng Puppyt.", and aligned export filenames plus a few persistent identifiers to the new name.
- Verified in-browser that the document title now reads `Bitruvius` and the header shows the new brand/subtitle while the mannequin continues animating normally.
- Phase-key authoring pass: added normalized sparse `phaseKeyframes`, a clickable phase timeline with contact/extreme markers, marker-driven key seeding, and circular phase blending that only fills the span between adjacent keys.
- Wired the phase overlay into live render and export sampling so authored keys are additive biases on top of the procedural pose, and exports now serialize normalized phase data with derived `timeMs` compatibility fields.
- Verification status: `npm run build` passes. The local Chromium/Playwright browser harness initially encountered macOS permission/crashpad errors, but this was resolved by granting accessibility and automation permissions in System Settings (Security & Privacy -> Accessibility) and disabling crashpad via environment flags (`PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` with external Chrome). The phase-timeline interaction was then successfully verified in a Playwright session at `/tmp/onion-audit`.
- Seamless refinement: switched the phase interpolation from straight linear blending to eased smootherstep blending so edits to a key flow into neighboring phases without the obvious hinge at each anchor.
- Awareness pass: added pose-aware damping to the phase blend so lifted legs bias toward firmer, more stomp-prone transitions while lower/ground-near extremes stay looser and more fluid.
- Performance audit: production preview startup hits `DOMContentLoaded` at about `415ms`; the shipped JS bundle is `395KB` on disk (`129KB` transferred, `404KB` decoded). The slowest startup resources are the Tailwind CDN script and Google Fonts, not the local bundle.
- Performance audit: runtime stepping on the production preview stays near budget. Measured `advanceTime` bursts averaged about `14.6-15.4ms` per step for live walk, idle, poser/no keys, and poser/with keys, with the keyed authoring case staying essentially flat against the no-key baseline.
- Performance audit: export throughput is dominated by rasterization/encoding. On the production preview, `Loop Frames ZIP` took about `2.0s` for `6.3MB`, `Keyframes ZIP` about `0.44s` for `431KB`, `GIF` about `1.2s` for `1.6MB`, and `WebM` about `2.3s` for `240KB`.
- Performance audit: the highest-value optimization targets are the external Tailwind/font loads, followed by loop-frame export encoding. Phase-key blending itself did not show up as a measurable runtime hotspot in the browser probes.
- Key-pose cleanup: smoothed the phase-key interpolation further with eased Catmull-Rom sampling between anchors, then added stage onion skins for nearby keyframes so authored poses read as a soft trail instead of isolated snapshots.
- Verified in a single Playwright session at `/tmp/onion-audit`: created contact keys at `0%` and `50%`, scrubbed to `25%`, and confirmed the stage renders translucent onion-skin mannequins between the anchors while the timeline reports `2 keys`.
- Current gait-control audit: exercised the live browser model against the running app and captured per-control deltas.
- Weakest visible controls in the current walk model were `Step Expression`, `Pelvic Lean`, `Counter Rotation`, `Symmetry`, `Tendon Elasticity`, `Cycle Frequency`, and `Lateral Sway`; the stance/impact controls still work but read a little softer than the strongest sliders.
- Next pass: widen the envelope on `Step Expression`, `Frictional Drag`, `Impact Density`, and `Counter Rotation` so their max positions are more obviously distinct without blowing up the whole walk cycle.
- Follow-up verification after the tuning pass: `Step Expression`, `Frictional Drag`, `Impact Density`, `Counter Rotation`, `Symmetry`, and `Pelvic Lean` all now produce visibly stronger max poses in the live browser screenshots, and the state dump still updates normally.
- Build status after the gait tuning: `npm run build` passes.
